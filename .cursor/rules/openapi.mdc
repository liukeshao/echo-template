---
description: openapi规范文档
globs: 
alwaysApply: false
---
# OpenAPI 多文件结构规范

## 概述

本项目采用 OpenAPI 3.1 规范。

## 目录结构

```
docs/
└── openapi.yaml              # 主入口文件
```

## 核心原则

### 1. 响应类型设计
为每种数据类型创建专门的响应类型
禁止使用复杂的 `allOf` 组合：

| 响应类型 | 用途 | data字段类型 |
|---------|------|-------------|
| `UserResponse` | 单个用户信息 | `UserOutput` |
| `UsersResponse` | 用户列表 | `ListUsersOutput` |
| `UserStatsResponse` | 用户统计 | `UserStatsOutput` |
| `MessageResponse` | 操作消息 | `MessageResponse` |
| `AuthResponse` | 认证信息 | `AuthData` |

### 2. 忽略docs_handler.go 和 example_handler.go

## 开发工作流

### 添加新的API接口

1. 在 `docs/openapi.yaml` 完善相关信息

### Schema与Go类型对应
- Schema定义必须与 `pkg/types/` 中的Go类型严格对应
- 验证规则与Go struct tag保持一致
- 为每个字段提供清楚的描述和示例

