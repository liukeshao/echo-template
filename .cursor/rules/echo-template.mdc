---
description: 项目开发规范
globs: 
alwaysApply: false
---
# Echo Template 项目开发规范

## 技术栈
- **框架**: Echo v4 + Ent ORM + SQLite + Viper + Zog 验证

## 核心规范

### 1. DTO 命名约定
- **输入**: `XxxInput` (RegisterInput, CreateUserInput)
- **输出**: `XxxOutput` (UserOutput, AuthOutput)  
- **列表**: `ListXxxInput/Output` (ListUsersInput, ListUsersOutput)

### 2. 列表输出统一结构
```go
type ListXxxOutput struct {
    Xxx        []*XxxOutput `json:"xxx"`         // 使用复数名
    Total      int64        `json:"total"`       // int64支持大数据
    Page       int          `json:"page"`        // 页码从1开始
    PageSize   int          `json:"page_size"`   // 每页大小
    TotalPages int          `json:"total_pages"` // 总页数
}
```

### 3. 类型定义规范
- **位置**: 统一在 `pkg/types/` 目录，按模块分文件
- **验证**: 所有 Input 类型必须实现 `Validate() []*errors.ErrorDetail` 方法
- **错误**: 统一使用 `pkg/errors.ErrorDetail` 类型

```go
type RegisterInput struct {
    Username string `json:"username"`
    Email    string `json:"email"`
    Password string `json:"password"`
}

func (i *RegisterInput) Validate() []*errors.ErrorDetail {
    // 使用 Zog 验证，返回 ErrorDetail 切片
}
```

### 4. Handler 开发模式
```go
// 1. 注册Handler
func init() {
    Register(new(UserHandler))
}

// 2. 实现接口
func (h *UserHandler) Init(c *services.Container) error {
    h.userService = c.User // 依赖注入
    return nil
}

func (h *UserHandler) Routes(g *echo.Group) {
    api := g.Group("/api/v1/users")
    api.GET("", h.ListUsers)
    api.POST("", h.CreateUser)
}

// 3. Handler函数（只需简短中文注释）
// CreateUser 创建用户
func (h *UserHandler) CreateUser(c echo.Context) error {
    var in types.CreateUserInput
    if err := c.Bind(&in); err != nil {
        return errors.BadRequestError("参数格式错误")
    }
    
    if errs := in.Validate(); len(errs) > 0 {
        return ValidationError("验证失败", errs).JSON(c)
    }
    
    output, err := h.userService.Create(ctx, &in)
    if err != nil {
        return err
    }
    
    return Success(output).JSON(c)
}
```

### 5. 服务容器架构
```go
type Container struct {
    ORM    *ent.Client
    Config *config.Config
    Web    *echo.Echo
    
    // 业务服务
    Auth *AuthService
    User *UserService
}
```

### 6. 响应格式规范
```go
type Response struct {
    Code      int                   `json:"code"`                 // 0=成功, 非0=失败
    Message   string                `json:"message"`              // 响应消息
    Data      interface{}           `json:"data"`                 // 响应数据
    Errors    []*errors.ErrorDetail `json:"errors,omitempty"`     // 错误详情
    Timestamp int64                 `json:"timestamp"`            // 时间戳
    RequestID string                `json:"request_id,omitempty"` // 请求ID
}

// 使用方式
return Success(data).JSON(c)
return ValidationError("验证失败", errorDetails).JSON(c)
```

## 文档规范（重要）

### OpenAPI 文档优先原则
**核心要求：Handler 开发完成后立即编写 OpenAPI 文档**

### Schema 命名约定
- Input: `CreateUserInput`, `UpdateUserInput`, `ListUsersInput`
- Output: `UserOutput`, `ListUsersOutput`  
- Response: `UserResponse`, `UsersResponse`

### 开发流程
1. 开发 Handler（简短中文注释）
2. **强制立即**编写 OpenAPI 文档
3. 验证文档语法：`yamllint docs/openapi.yaml`
4. 测试 API：`curl http://localhost:8000/openapi.json`

## 项目结构
```
pkg/
├── handlers/   # HTTP 处理器
├── services/   # 业务服务层  
├── middleware/ # 中间件
├── types/      # Input/Output 定义
├── errors/     # 错误处理
├── context/    # 上下文处理
├── log/        # 日志系统
└── utils/      # 工具函数
```

## 开发最佳实践

### 验证与错误
- 所有 Input 实现 `Validate()` 方法
- 统一使用 `[]*errors.ErrorDetail` 格式
- 使用 Zog 进行类型安全验证

### 依赖注入
- 服务通过容器注册和注入
- Handler 通过 `Init()` 方法接收依赖
- 避免在 Handler 中直接创建服务

### 日志
```go
import "log/slog"
slog.InfoContext(ctx, "处理请求", "user_id", userID)
slog.ErrorContext(ctx, "业务错误", "error", err)
```

### 常用命令
```bash
make ent-new name=EntityName  # 新建实体
make ent-gen                  # 生成代码
go run cmd/web/main.go        # 启动服务
yamllint docs/openapi.yaml   # 验证文档
```

## 核心原则
- **类型安全**: Input 验证 + 统一错误格式
- **依赖注入**: 容器管理 + 接口抽象  
- **文档优先**: OpenAPI 强制 + 代码同步
- **简洁至上**: 避免过度工程化

---
记住：快速开发，简洁代码，文档与代码同步！ 