---
description: 项目开发规范
globs: 
alwaysApply: false
---
# Echo Template 项目 Cursor 规则

## 项目概述
这是一个基于 Go 的高性能 Web 应用模板，使用以下技术栈：
- **Web 框架**: Echo v4 - 高性能、可扩展、极简的 Go Web 框架
- **ORM**: Ent - 简单而强大的实体框架，用于数据建模和查询
- **数据库**: SQLite - 轻量级、高可靠性的嵌入式数据库
- **配置管理**: Viper - 灵活的配置管理库
- **测试**: Testify - Go 测试断言库

## 核心设计规范

### 1. 数据传输对象 (DTO) 命名规范

#### Input/Output 命名约定
所有API数据传输对象必须遵循以下命名规范：
- **输入类型**：使用 `xxxInput` 后缀，例如：`RegisterInput`、`LoginInput`、`CreateUserInput`
- **输出类型**：使用 `xxxOutput` 后缀，例如：`AuthOutput`、`UserOutput`、`ListUsersOutput`

#### 类型定义位置
- 所有输入输出类型统一定义在 `pkg/types/` 目录下
- 按功能模块分文件组织，例如：
  - `pkg/types/auth.go` - 认证相关的输入输出类型
  - `pkg/types/user.go` - 用户相关的输入输出类型
  - `pkg/types/order.go` - 订单相关的输入输出类型

#### 类型定义示例
```go
// pkg/types/auth.go
package types

import "time"

// RegisterInput 用户注册输入
type RegisterInput struct {
    Username string `json:"username" validate:"required,min=3,max=50"`
    Email    string `json:"email" validate:"required,email"`
    Password string `json:"password" validate:"required,min=8"`
}

// LoginInput 用户登录输入
type LoginInput struct {
    Email    string `json:"email" validate:"required,email"`
    Password string `json:"password" validate:"required"`
}

// AuthOutput 认证输出
type AuthOutput struct {
    User         *UserInfo `json:"user"`
    AccessToken  string    `json:"access_token"`
    RefreshToken string    `json:"refresh_token"`
    ExpiresAt    int64     `json:"expires_at"`
}

// UserInfo 用户信息（可在多个Output中复用）
type UserInfo struct {
    ID          string     `json:"id"`
    Username    string     `json:"username"`
    Email       string     `json:"email"`
    Status      string     `json:"status"`
    LastLoginAt *time.Time `json:"last_login_at,omitempty"`
    CreatedAt   time.Time  `json:"created_at"`
}
```

#### 服务层方法签名规范
```go
// 服务层方法必须使用types包中的Input/Output类型
func (s *AuthService) Register(ctx context.Context, in *types.RegisterInput) (*types.AuthOutput, error)
func (s *AuthService) Login(ctx context.Context, in *types.LoginInput) (*types.AuthOutput, error)
func (s *UserService) CreateUser(ctx context.Context, in *types.CreateUserInput) (*types.UserOutput, error)
```

#### Handler层使用规范
```go
// Handler层统一使用types包中的类型
func (h *AuthHandler) Register(c echo.Context) error {
    var in types.RegisterInput
    if err := c.Bind(&in); err != nil {
        return errors.BadRequestError("请求参数格式错误").With("error", err.Error())
    }
    
    output, err := h.authService.Register(ctx, &in)
    if err != nil {
        return err
    }
    
    return Success(output).JSON(c)
}
```

#### 验证标签规范
输入类型必须包含适当的验证标签：
```go
type CreateUserInput struct {
    Username string `json:"username" validate:"required,min=3,max=50"`
    Email    string `json:"email" validate:"required,email"`
    Password string `json:"password" validate:"required,min=8"`
    Age      int    `json:"age" validate:"min=0,max=120"`
}
```

## 代码规范

### 2. 项目结构规范
```
/
├── cmd/           # 应用程序入口点
├── pkg/           # 核心业务逻辑
│   ├── handlers/  # HTTP 路由处理器
│   ├── services/  # 业务服务层
│   ├── middleware/ # 中间件（统一放置在此目录）
│   ├── context/   # 上下文处理
│   ├── errors/    # 错误处理
│   ├── log/       # 日志系统
│   ├── utils/     # 工具函数
│   └── tests/     # 测试辅助工具
├── config/        # 配置文件和结构
├── docs/          # 文档
└── ent/           # Ent ORM 生成代码和 schema
```

### 3. Handler 开发规范

#### Handler 接口
所有 Handler 必须实现以下接口：

```go
type Handler interface {
    // Routes 允许自注册 HTTP 路由到路由器
    Routes(g *echo.Group)
    
    // Init 提供服务容器进行初始化
    Init(*services.Container) error
}
```

#### Handler 开发模式

**自动注册机制**：
```go
// 1. 使用 init 函数注册 Handler
func init() {
    Register(new(UserHandler))
}

// 2. 实现 Init 方法（依赖注入）
func (h *UserHandler) Init(c *services.Container) error {
    h.orm = c.ORM
    h.config = c.Config
    h.authService = c.Auth        // 从容器注入服务
    h.userService = c.User        // 从容器注入服务
    return nil
}

// 3. 实现 Routes 方法（路由定义）
func (h *UserHandler) Routes(g *echo.Group) {
    api := g.Group("/api/v1/users")
    
    // RESTful 路由
    api.GET("", h.GetUsers)
    api.POST("", h.CreateUser)
    api.GET("/:id", h.GetUser)
    api.PUT("/:id", h.UpdateUser)
    api.DELETE("/:id", h.DeleteUser)
}
```

#### 错误处理模式
```go
func (h *Handler) SomeMethod(c echo.Context) error {
    // 直接返回 AppError，由错误处理器统一处理
    return errors.NotFoundError("资源不存在").
        With("resource_id", resourceID)
}
```

#### 日志记录模式
```go
func (h *Handler) SomeMethod(c echo.Context) error {
    ctx := c.Request().Context()
    
    slog.InfoContext(ctx, "操作开始", "param", value)
    // 业务逻辑...
    slog.InfoContext(ctx, "操作完成", "result", result)
    
    return Success(result).JSON(c)
}
```

### 4. 中间件开发规范

#### 中间件目录结构
```
pkg/middleware/
├── auth.go          # 认证中间件
├── rate_limit.go    # 限流中间件
├── request_id.go    # 请求ID中间件
├── cors.go          # CORS中间件（如需要）
├── security.go      # 安全头中间件（如需要）
└── logging.go       # 访问日志中间件（如需要）
```

#### 中间件基础模式
```go
// 简单中间件
func MiddlewareName(next echo.HandlerFunc) echo.HandlerFunc {
    return func(c echo.Context) error {
        // 前置处理逻辑
        err := next(c)
        // 后置处理逻辑（可选）
        return err
    }
}

// 带配置的中间件
type Middleware struct {
    config MiddlewareConfig
}

func NewMiddleware(config MiddlewareConfig) *Middleware {
    return &Middleware{config: config}
}

func (m *Middleware) Handler(next echo.HandlerFunc) echo.HandlerFunc {
    return func(c echo.Context) error {
        // 使用 m.config 中的配置
        return next(c)
    }
}
```

#### 认证中间件使用
```go
// 在Handler中使用认证中间件
func (h *UserHandler) Routes(g *echo.Group) {
    // 公开路由
    api := g.Group("/api/v1/users")
    api.GET("/public", h.GetPublicUsers)
    
    // 需要认证的路由
    authMw := middleware.NewAuthMiddleware(h.orm)
    protected := g.Group("/api/v1/users")
    protected.Use(authMw.RequireAuth)
    protected.GET("", h.GetUsers)
    protected.POST("", h.CreateUser)
    
    // 可选认证的路由
    optional := g.Group("/api/v1/users")
    optional.Use(authMw.OptionalAuth)
    optional.GET("/recommendations", h.GetRecommendations)
}

// 从context获取用户信息
user, ok := middleware.GetUserFromEcho(c)
if !ok {
    return errors.UnauthorizedError("用户未登录")
}
```

#### 中间件最佳实践
- 使用统一的错误处理：`errors.BadRequestError("验证失败")`
- 记录详细日志：`slog.InfoContext(ctx, "中间件执行", "middleware", "AuthMiddleware")`
- 异步处理非关键任务：`go func() { /* 异步逻辑 */ }()`
- Context值传递：使用typed key避免冲突

### 5. 数据库和 ORM 规范

#### Ent Entity 开发
```bash
# 创建新实体
make ent-new name=EntityName

# 生成代码
make ent-gen
```

#### 查询模式
```go
entity, err := c.ORM.EntityType.
    Query().
    Where(entitytype.FieldName(value)).
    Where(entitytype.HasRelationWith(relation.ID(relationID))).
    Only(ctx.Request().Context())
```

### 6. 服务容器规范

#### 服务容器架构
服务容器是项目的依赖注入核心，管理所有服务的生命周期：

```go
type Container struct {
    ORM    *ent.Client          // 数据库ORM客户端
    Config *config.Config       // 配置管理
    Web    *echo.Echo          // Web框架实例
    
    // 业务服务（通过容器注入）
    Auth   *services.AuthService    // 认证服务
    User   *services.UserService    // 用户服务
    // 其他业务服务...
}
```

#### 依赖注入最佳实践
- **所有服务都在容器中注册**：避免在Handler中直接创建服务实例
- **Handler通过Init()方法注入依赖**：保持依赖关系明确和可测试
- **服务之间的依赖通过容器管理**：避免循环依赖

#### Handler依赖注入模式
```go
// ✅ 正确：从容器注入服务
func (h *AuthHandler) Init(c *services.Container) error {
    h.orm = c.ORM
    h.auth = c.Auth        // 从容器获取已注册的服务
    return nil
}

// ❌ 错误：在Handler中直接创建服务
func (h *AuthHandler) Init(c *services.Container) error {
    h.orm = c.ORM
    h.authService = services.NewAuthService(c.ORM)  // 避免这样做
    return nil
}
```

#### 服务注册模式
```go
// 在服务容器中注册所有业务服务
func NewContainer() *Container {
    c := &Container{}
    
    // 初始化基础服务
    c.Config = config.Load()
    c.ORM = setupDatabase(c.Config)
    c.Web = echo.New()
    
    // 注册业务服务
    c.Auth = NewAuthService(c.ORM)
    c.User = NewUserService(c.ORM)
    // ... 其他服务
    
    return c
}
```

#### 测试中的容器使用
```go
func TestHandler(t *testing.T) {
    // 创建测试容器
    c := services.NewTestContainer()
    defer c.Shutdown()
    
    // 使用完整的容器进行集成测试
    handler := &UserHandler{}
    err := handler.Init(c)
	require.NoError(t, err)
}
```

### 7. 日志规范

#### 使用结构化日志
```go
import "log/slog"

// 推荐：使用 Context 版本，自动注入 request_id
slog.InfoContext(ctx, "处理用户请求", "user_id", userID)
slog.ErrorContext(ctx, "业务逻辑错误", "error", err)
slog.DebugContext(ctx, "查询数据库", "table", "users", "query", sql)
slog.WarnContext(ctx, "性能警告", "duration_ms", 2000)
```

#### AppError 日志集成
```go
// AppError 会自动展开为结构化字段
appErr := errors.NotFoundError("用户不存在").
    With("user_id", 123, "attempted_at", time.Now())

slog.ErrorContext(ctx, "业务错误", "error", appErr)
```

### 8. 错误处理规范

#### 统一错误结构
```go
// HTTP 错误
errors.BadRequestError("请求参数无效")
errors.UnauthorizedError("未授权访问")
errors.ForbiddenError("权限不足")
errors.NotFoundError("资源不存在")
errors.InternalError("服务器内部错误")

// 业务错误
errors.DatabaseError("数据库连接失败").Wrap(err)
errors.ValidationError("字段验证失败").With("field", "email")
```

#### 错误处理模式
```go
func (h *Handler) GetUser(c echo.Context) error {
    user, err := h.userService.GetByID(userID)
    if err != nil {
        // 直接返回AppError，由错误处理器统一处理
        return errors.NotFoundError("用户不存在").
            With("user_id", userID)
    }
    return Success(user).JSON(c)
}
```

### 9. 响应处理规范

#### 统一响应格式
```go
// 成功响应
return Success(data).JSON(c)

// 验证错误响应
errorDetails := []ErrorDetail{
    {Field: "email", Message: "邮箱格式不正确", Code: "INVALID_EMAIL"},
}
return ValidationError("验证失败", errorDetails).JSON(c)

// 自定义响应
return NewResponse().
    WithCode(errors.OK).
    WithMessage("操作成功").
    WithData(result).
    JSON(c)
```

### 10. 认证系统使用规范

#### JWT Token管理
```go
// 生成token
accessToken, err := utils.GenerateAccessToken(userID)
refreshToken, err := utils.GenerateRefreshToken(userID)

// 验证token
claims, err := utils.ValidateToken(tokenString)
if err != nil || utils.IsTokenExpired(claims) {
    return errors.UnauthorizedError("令牌无效或已过期")
}
```

#### 用户注册登录（使用容器注入的服务）
```go
// 通过容器注入的AuthService进行操作
func (h *AuthHandler) Register(c echo.Context) error {
    // h.authService 已通过容器注入
    response, err := h.authService.Register(ctx, &types.RegisterInput{
        Username: "user123",
        Email:    "user@example.com", 
        Password: "password123",
    })
    
    if err != nil {
        return err
    }
    
    return Success(response).JSON(c)
}

func (h *AuthHandler) Login(c echo.Context) error {
    // h.authService 已通过容器注入
    response, err := h.authService.Login(ctx, &types.LoginInput{
        Email:    "user@example.com",
        Password: "password123", 
    })
    
    if err != nil {
        return err
    }
    
    return Success(response).JSON(c)
}
```

## 编码最佳实践

### Go 代码风格
- 遵循 Go 官方代码规范
- 使用 `gofmt` 格式化代码
- 导入顺序：标准库 -> 第三方 -> 项目内部

### 错误处理
- 始终检查并处理错误
- 使用包装错误提供上下文
- 记录错误但不重复记录

### 并发安全
- 数据库连接是并发安全的
- 避免共享可变状态
- 适当使用互斥锁

### 性能考虑
- 复用数据库连接
- 使用上下文进行超时控制
- 避免 N+1 查询问题
- 中间件中避免重型操作，使用异步处理

### 依赖管理
- **避免在Handler中直接创建服务实例**
- **通过服务容器管理所有依赖关系**
- **保持服务的单例模式**，避免重复创建
- **服务之间的依赖通过构造函数注入**

## 开发工作流

### 1. 新功能开发
1. 如需新服务，先在服务容器中注册
2. 创建 Handler 文件，通过Init()方法注入依赖
3. 定义路由和处理逻辑
4. 如需新实体，创建 Ent schema
5. 编写测试
6. 更新配置（如需要）

### 2. 中间件开发
1. 在 `pkg/middleware` 创建中间件文件
2. 遵循中间件开发规范
3. 在相应Handler中注册使用
4. 编写单元测试
5. 更新文档

### 3. 服务开发
1. 在 `pkg/services` 创建服务文件
2. 在服务容器中注册新服务
3. 更新需要使用该服务的Handler的Init()方法
4. 编写服务的单元测试
5. 更新文档

### 4. 数据库变更
1. 修改 Ent schema
2. 运行 `make ent-gen`
3. 测试迁移
4. 更新相关 Handler 和 Service

## 常用命令
```bash
# Ent 相关
make ent-install  # 安装 Ent 工具
make ent-new name=EntityName  # 创建新实体
make ent-gen      # 生成 Ent 代码

# 开发
go run cmd/main.go  # 启动应用
go test ./...       # 运行所有测试
go build ./...      # 编译项目
```

## 注意事项

### 安全
- 始终验证用户输入
- 使用参数化查询防止 SQL 注入
- 实施适当的身份验证和授权
- 敏感信息不要记录到日志

### 可维护性
- 保持函数简短和专注
- 使用有意义的变量和函数名
- 编写文档说明复杂逻辑
- 中间件职责单一，便于测试
- **服务通过容器注入，便于Mock和测试**

### 扩展性
- 使用接口而不是具体类型
- 保持松耦合设计
- 考虑未来的扩展需求
- 中间件支持配置化
- **新服务通过容器注册，易于扩展**

### 依赖注入原则
- **单一职责**：每个服务只负责一个领域
- **依赖倒置**：依赖抽象接口而非具体实现
- **容器管理**：所有依赖通过容器统一管理
- **生命周期控制**：容器负责服务的创建和销毁

记住：这个项目的目标是快速、简单的开发。保持代码简洁，优先选择简单的解决方案。中间件应该专注单一职责，便于组合和测试。服务通过容器注入，保持松耦合和高可测试性。
